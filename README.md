To run tests use gradle wrapper:
`./gradlew runTests -Dtag=<tag>`.

Report will generate to folder `build/reports/allure-report/<tag>`

Where tag equals to `all` (run all tests) or `smoke` (only smoke tests).

